# sozai

素材 — core utilities for the stack (async, codec, event, execution, flow,
generator, log, otel, patch, result, runtime, schema, stream, runtime-expo).

See the monorepo-split design (`../kigu/docs/repo-split-design.md`) for context.
